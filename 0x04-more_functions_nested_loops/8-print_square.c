#include "main.h"

/**
 * print_square - prints a square
 *
 * @size: funtion parameter
 *
 * Return: void
 */
void print_square(int size)
{
	int a;
	int b;

	for (a = 0; a < size; a++)
	{
		for (b = 0; b < size; b++)
		{
			_putchar(35);
		}
		if (i != size - 1)
			_putchar('\n');
	}

	_putchar('\n');
}
